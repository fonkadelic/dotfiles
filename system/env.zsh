set -o vi
export EDITOR="$HOME/.dotfiles/bin/set-editor"   # $EDITOR should open in terminal
export VISUAL="open -a Emacs.app"                # $VISUAL opens in GUI with non-daemon as alternate
export PAGER="less"
